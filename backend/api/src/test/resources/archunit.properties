# Allow ArchUnit rules to pass when no classes match the that() predicate.
# Needed for Phase 0: rules targeting packages that don't exist yet (e.g. odin.storage)
# should pass trivially rather than failing with "no classes checked".
# Remove or set to true when all target packages are populated.
archRule.failOnEmptyShould=false
